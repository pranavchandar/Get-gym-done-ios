Golden vectors generated from the web app's TypeScript implementations land here in
Phase 1 (see docs/ACTION-PLAN.md task 1.13). Each file is a list of input/output pairs
produced by running the original functions over pseudo-random inputs; the Swift ports
must reproduce them exactly.

This placeholder keeps the directory present so SwiftPM's resource copy has a target.
